# Copy to `.env` and fill in your real values. `.env` is gitignored.

# Required: SEC EDGAR fair-access policy requires a descriptive User-Agent
# with a real contact email. The SEC may rate-limit or block requests that
# don't include one. Format: "Application Name your.email@example.com"
SEC_USER_AGENT="SEC Financials App you@example.com"
